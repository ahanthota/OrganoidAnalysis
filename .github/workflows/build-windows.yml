name: Build Windows Application

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: Build with PyInstaller (OneFile - Single EXE)
      run: |
        pyinstaller desktop_app_onefile.spec --clean
    
    - name: Build with PyInstaller (OneDir - Folder)
      run: |
        pyinstaller desktop_app.spec --clean
    
    - name: Create ZIP archive for OneDir version
      run: |
        Compress-Archive -Path dist\OrganoidAnalysis\* -DestinationPath OrganoidAnalysis-Windows-Folder.zip -Force
    
    - name: Copy OneFile EXE for distribution
      run: |
        Copy-Item dist\OrganoidAnalysis.exe -Destination OrganoidAnalysis.exe
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: OrganoidAnalysis-Windows
        path: |
          dist/OrganoidAnalysis/
          dist/OrganoidAnalysis.exe
          OrganoidAnalysis-Windows-Folder.zip
          OrganoidAnalysis.exe
        retention-days: 30
    
    - name: Create Release (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          OrganoidAnalysis.exe
          OrganoidAnalysis-Windows-Folder.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

